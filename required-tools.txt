# Tool names the compiled catalog MUST resolve, verified at image build
# by `toolcatalog verify` (a gap fails the build instead of a boot job
# or an enable-time error in Settings -> Tools). Two groups:
#
# 1. The shipped seed templates (toolbelt.DefaultSeed): the disabled
#    LSP quartet + gh that first-boot manifests carry.
# 2. The backend runtimes the engine auto-adopts as dependencies
#    (go: installs need go, npm: needs node, python tools need uv).
#
# seed
gopls
typescript-language-server
typescript
pyright
rust-analyzer
gh
# backend runtimes (auto-adopted dependencies)
go
node
uv
