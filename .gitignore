.code-review/
.refactor/

# Compiled Go binary (regenerated by `go build`); should not be committed.
vibekit
node_modules/
static-src/node_modules/
static-src/node_modules/.vite/
static-src/node_modules/.vite-temp/
.vitest-cache/
# TypeScript build output and Dockerfile-fetched vendor assets;
# regenerated by tsgo + the Dockerfile build stage in prod.
static/*.js
static/wire/
static/handlers/
static/vendor/
# tsgo emits compiled .js mirroring the static-src module tree into
# these subdirectories; all build output, never committed.
static/actions/
static/lib/
static/__test-helpers__/

static/style.css

# Kiro skill artifacts (code-review, refactor, steering-maintenance)
**/.steering-maintenance/
