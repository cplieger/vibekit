{
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage",
    "typecheck": "tsgo -config tsconfig.json",
    "typecheck:tests": "tsgo -config tsconfig.test.json"
  },
  "devDependencies": {
    "@types/node": "25.9.1",
    "@vitest/coverage-v8": "4.1.7",
    "fast-check": "4.8.0",
    "happy-dom": "20.9.0",
    "vitest": "4.1.7"
  }
}
