name: Build and Push

on:
  push:
    branches: [main]
    paths:
      - "Dockerfile"
      - "**.go"
      - "go.mod"
      - "static-src/**"
      - "static/**"
      - "!go.sum"
      - "entrypoint.sh"
      - "setup-tools.sh"
      - "tools.json"
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write
  security-events: write

jobs:
  release:
    uses: cplieger/ci/.github/workflows/docker-release.yaml@38aa4e20293b20ff3f707553f46f54e287c97b5f # v1
    with:
      registries: ghcr
      skip-build-guard: true
      sbom-from: path
      notify-repo: cplieger/homelab
    secrets: inherit
