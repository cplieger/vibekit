/** Marks an element whose text belongs to the UI, not the agent. Presence only.
 *  Declared by the module that renders the affordance, so a new one is excluded
 *  by its own author rather than by a class list kept in the reader. */
export const CHROME_ATTR = "data-vk-chrome";
