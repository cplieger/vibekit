{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>cplieger/.github"],
  "packageRules": [
    {
      "description": "tsgo / TypeScript 7 native preview. Currently in active prerelease (versions are all 7.0.0-dev.<date>); the latest dist-tag is the only one with up-to-date Go-stdlib CVE fixes, so we follow it and suppress per-bump Docker rebuilds via the [skip-build] suffix. CLEANUP WHEN TS 7 IS STABLE: drop followTag, drop commitMessageSuffix, restore default release cadence; @typescript/native-preview is expected to ship a stable major eventually (track https://github.com/microsoft/typescript-go/milestones). Also remove the matching if-guard in .github/workflows/build.yaml.",
      "matchPackageNames": ["@typescript/native-preview"],
      "followTag": "latest",
      "commitMessageSuffix": " [skip-build]",
      "ignoreUnstable": false
    }
  ],
  "customDatasources": {
    "kiro-cli": {
      "defaultRegistryUrlTemplate": "https://desktop-release.q.us-east-1.amazonaws.com/index.json",
      "format": "json",
      "transformTemplates": [
        "{ \"releases\": versions.{ \"version\": version, \"digest\": packages[fileType = 'zip' and targetTriple = 'x86_64-unknown-linux-gnu' and variant = 'headless'][0].sha256 } }"
      ]
    }
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "vibekit tools.json — github-releases entries (version followed by update.method github + repo)",
      "managerFilePatterns": ["/tools\\.json$/"],
      "matchStrings": [
        "\"version\":\\s*\"(?<currentValue>[^\"]+)\",\\s*\"update\":\\s*\\{\\s*\"method\":\\s*\"github\",\\s*\"repo\":\\s*\"(?<depName>[^\"]+)\""
      ],
      "datasourceTemplate": "github-releases"
    },
    {
      "customType": "regex",
      "description": "vibekit tools.json — npm entries (version followed by update.method npm + package)",
      "managerFilePatterns": ["/tools\\.json$/"],
      "matchStrings": [
        "\"version\":\\s*\"(?<currentValue>[^\"]+)\",\\s*\"update\":\\s*\\{\\s*\"method\":\\s*\"npm\",\\s*\"package\":\\s*\"(?<depName>[^\"]+)\""
      ],
      "datasourceTemplate": "npm"
    },
    {
      "customType": "regex",
      "description": "vibekit tools.json — gomod entries (version followed by update.method gomod + module)",
      "managerFilePatterns": ["/tools\\.json$/"],
      "matchStrings": [
        "\"version\":\\s*\"(?<currentValue>[^\"]+)\",\\s*\"update\":\\s*\\{\\s*\"method\":\\s*\"gomod\",\\s*\"module\":\\s*\"(?<depName>[^\"]+)\""
      ],
      "datasourceTemplate": "go"
    },
    {
      "customType": "regex",
      "description": "vibekit tools.json — rubygems entries (version followed by update.method rubygems + package)",
      "managerFilePatterns": ["/tools\\.json$/"],
      "matchStrings": [
        "\"version\":\\s*\"(?<currentValue>[^\"]+)\",\\s*\"update\":\\s*\\{\\s*\"method\":\\s*\"rubygems\",\\s*\"package\":\\s*\"(?<depName>[^\"]+)\""
      ],
      "datasourceTemplate": "rubygems"
    },
    {
      "customType": "regex",
      "description": "kiro-cli version + sha256 pinned in entrypoint.sh",
      "managerFilePatterns": ["/entrypoint\\.sh$/"],
      "matchStrings": [
        "#\\s*renovate:\\s*datasource=(?<datasource>custom\\.kiro-cli)\\s+depName=(?<depName>[^\\s]+)\\s*\\nKIRO_CLI_VERSION=\"(?<currentValue>[^\"]+)\"\\s*\\nKIRO_CLI_SHA256=\"(?<currentDigest>[^\"]+)\""
      ],
      "versioningTemplate": "semver"
    }
  ]
}
