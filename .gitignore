.code-review/

# Compiled Go binary (regenerated by `go build`); should not be committed.
web/vibekit
web/static-src/node_modules/
.vitest-cache/
web/static/style.css

# Ad-hoc puppeteer smoke / repro tests live alongside the scaffolding
# in web/static-src/puppeteer/ but are never committed; only README.md
# in that directory is tracked.
web/static-src/puppeteer/*.cjs
web/static-src/puppeteer/*.mjs
web/static-src/puppeteer/*.js
